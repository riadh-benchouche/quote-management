{% extends 'partials/layout.html.twig' %}

{% block title %}Categorie
{% endblock %}

{% block main %}
	<h1>Categorie</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ categorie.id }}</td>
			</tr>
			<tr>
				<th>Nom</th>
				<td>{{ categorie.nom }}</td>
			</tr>
		</tbody>
	</table>

	<a href="{{ path('front_app_categorie_index') }}">front to list</a>

	<a href="{{ path('front_app_categorie_edit', {'id': categorie.id}) }}">edit</a>

	{{ include('front/categorie/_delete_form.html.twig') }}
{% endblock %}
